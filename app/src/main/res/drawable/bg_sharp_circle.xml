<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <!-- 填充颜色 -->
    <solid android:color="@color/transparent" />

    <!-- 线的宽度，颜色灰色 -->
    <stroke
        android:width="1dp"
        android:color="@color/grey_d7d7d7" />

    <size
        android:width="16dp"
        android:height="16dp" />

</shape>