<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blue_0090FF"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/search_result_tab_back_iv"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingLeft="15dp"
            android:src="@mipmap/nav_btn_back" />

        <!--app:tabSelectedTextColor改变选中字体的颜色-->
        <!--app:tabTextColor改变未选中字体的颜色-->
        <!--app:tabIndicatorColor改变指示器下标的颜色-->
        <!--app:tabIndicatorHeight设置指示器下标的高度-->
        <!--app:tabMode="scrollable"可滑动-->
        <android.support.design.widget.TabLayout
            android:id="@+id/search_result_tablayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            app:tabGravity="center"
            app:tabIndicatorColor="@color/colorAccent"
            app:tabIndicatorHeight="4dp"
            app:tabMode="fixed"
            app:tabPadding="10dp"
            app:tabSelectedTextColor="@color/colorAccent"
            app:tabTextColor="@color/white" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingRight="15dp"
            android:src="@mipmap/nav_btn_back"
            android:visibility="invisible" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/search_result_tab_vp"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/white" />
</LinearLayout>